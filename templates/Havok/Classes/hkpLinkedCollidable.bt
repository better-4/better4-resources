typedef struct
{
    hkpCollidable super;
    hkArray m_collisionEntries;

    /*
    if (m_collisionEntries.m_size > 0)
    {
        SkipTo(16);
        LinkMarker entryLinks[m_collisionEntries.m_size] <optimize=false>;
    }
    */

} hkpLinkedCollidable;