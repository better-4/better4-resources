//------------------------------------------------
// Inline pack struct
// (THAW-like 0x4A item)
//------------------------------------------------

typedef struct
{
    ushort structSize;
    SkipTo(4);
    ubyte structBytes[structSize];
} QBInlinePackStruct;