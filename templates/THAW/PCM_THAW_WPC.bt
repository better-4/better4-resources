//------------------------------------------------
// PCM .dat file.
//
// (THAW WPC)
//------------------------------------------------

#include "PCM.bt"

local ubyte test_byte = ReadByte();

if (test_byte == 0)
    BigEndian();
else
    LittleEndian();

PCMVersion = PCMVERSION_THAW;
PCMFile pcm;