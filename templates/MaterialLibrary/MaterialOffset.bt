// Not all too sure what this is

typedef struct (int matCount, uint MAT_START)
{
    local uint i;

    for (i=0; i<matCount; i++)
        RelativeOffset pt_template(MAT_START) <bgcolor=CL_POINTER>;

} MaterialOffsetBlock;