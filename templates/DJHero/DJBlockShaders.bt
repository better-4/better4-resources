//------------------------------------------------
// DJ HERO 1 MSB: SHADER BLOCK
// Contains info about shaders used I guess
//------------------------------------------------

typedef struct
{
    uint stringCount <bgcolor=CL_SINGLEVALUE>;
    StringWithLen strings[stringCount] <optimize=false>;
} DJBlockShaders;